{
  "dependencies": {
    "jsdom": "^22.1.0",
    "hono": "^3.6.3"
  },
  "scripts": {
    "dev": "bun run src/bundle.ts & make sass & bun run --hot src/server.ts",
    "bundle": "bun build --watch --splitting --target browser --sourcemap --outdir ./public/scripts src/scripts/*"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.3",
    "bun-types": "^1.0.2",
    "sass": "^1.69.5"
  }
}
